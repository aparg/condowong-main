import ScheduleTime from "@/components/ScheduleTime";
import React from "react";

const page = () => {
  return (
    <div className="container">
      <ScheduleTime />
    </div>
  );
};

export default page;
