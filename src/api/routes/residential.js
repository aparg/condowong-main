import { BASE_URL_RETS } from "../index";

export const resenditialAndCondo = {
  properties: `${BASE_URL_RETS}/residential/Properties/$query`,
  photos: `${BASE_URL_RETS}/residentialPhotos/MLS-index.jpeg`,
};
